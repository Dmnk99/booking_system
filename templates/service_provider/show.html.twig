{% extends 'base.html.twig' %}

{% block title %}ServiceProvider{% endblock %}

{% block body %}
    <h1>ServiceProvider</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_provider.id }}</td>
            </tr>
            <tr>
                <th>BusinessName</th>
                <td>{{ service_provider.businessName }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ service_provider.adress }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service_provider.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_provider_index') }}">back to list</a>

    <a href="{{ path('app_service_provider_edit', {'id': service_provider.id}) }}">edit</a>

    {{ include('service_provider/_delete_form.html.twig') }}
{% endblock %}
